[project]
name = "agent-development-kit-poc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-adk>=1.14.1",
]

[dependency-groups]
dev = [
    "ruff>=0.13.1",
]
